// *INDENT-OFF*
#pragma GCC optimize("Ofast") 
#pragma GCC target("avx,avx2,fma")

#include<bits/stdc++.h>
#define first F
#define second S
#define pb push_back
#define pback pop_back
#define all(x) x.begin(), x.end()
#define int long long
using namespace std;
int i = 0, j = 0, MOD = 1e7;
template<typename T>T prime_check_sqrt(const T& x) { int count = 0, i = 0; for(i = 1; i*i <= x; ++i) {if(x % i == 0) {++count; if(i != x/i) { ++count; }}} return count;}
template<typename T>T prime_factor(const T& x){int count = 0, i = 0; for(i = 2; i*i <= x; ++i){ if(x % i == 0){++count; while(x % i == 0){ x /= i; }}}if(x > 1){++count;} return count;}
// *INDENT-ON*

void solve()
{
    
    return;
}

signed main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    cout << '\n';
    int tt = 1, count = 1;
    /* cin >> tt; */
    while(tt--) {
        /* cout << "Case #" << count << ": "; */
        solve();
        /* cout << '\n' */;
        /* ++count; */
    }
    return 0;
}
endsnippet

